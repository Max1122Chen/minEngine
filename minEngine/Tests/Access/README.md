# TestAccess specializations (TEST-F04)

Per-type `minEngine::Testing::TestAccess<T>` specializations live here.
Include only from `Tests/` TUs — never from Runtime production headers.

See `docs/ai/Platform/Test/TEST-F04_TEST_ACCESS_DESIGN.md`.
