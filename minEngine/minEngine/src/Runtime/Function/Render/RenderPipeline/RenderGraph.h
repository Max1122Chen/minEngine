#pragma once
#include "Core.h"

namespace minEngine
{
    class RenderGraph
    {
    public:
        RenderGraph() = default;
        ~RenderGraph() = default;
    private:
        // std::vector<std::unique_ptr<RenderPassBase>> m_RenderPasses;
    };
}