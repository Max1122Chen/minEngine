#include "Render/RenderGraph/RenderGraph.h"

// ExecuteGraph lives in RenderGraph.cpp (RND-F01 S01).
// This translation unit anchors the manual execution slice for future growth
// (pass ordering helpers, validation hooks before Bake in S05).

namespace minEngine
{
}
