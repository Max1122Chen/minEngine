# Granite-style RDG + Frame Resource Ownership ? Design Spec

## Meta
- **ID:** `RND-F07`
- **Type:** Refactor
- **Status:** Done
- **Owner:** project maintainer
- **Last updated:** 2026-08-02
- **Related:** [Implementation](./RND-F07_GRANITE_RDG_RESOURCE_REFACTOR_IMPLEMENTATION.md), [FEATURE_REGISTRY](../FEATURE_REGISTRY.md), [ACTIVE_WORK](../ACTIVE_WORK.md), [RND-F01](./RND-F01_RENDER_GRAPH_DESIGN.md)?? Manual ??? Feature **??**? S06+ ?????, [RND-F06](./RND-F06_FORWARD_RENDERER_DESIGN.md)
- **Reference source (local):** `D:\Dev\GitRepo\Granite\renderer\render_graph.hpp` / `render_graph.cpp`?MIT???????????????????

## TL;DR

??????? RT / ?????? **Scene + ShadowResourceManager ????**?? `RegisterExternal` ? Manual ???? Bake ???????????**??** Granite RDG?? Feature ???**??????**???????????????????????????/?????? Granite ???? RDG????????????`bake` / `setup_attachments` / execute????? Pass / Renderer ?????**??**?? `RegisterExternal` ?????????? Bake?

## Scope
- **In:**
  - Phase 1???????????????????????? SceneColor/Depth?shadow atlas?post ping-pong ???
  - Phase 2?? Granite `RenderGraph` ???????Pass ???Bake ???????????????????????
  - ???? Spec + Implementation?Registry / ACTIVE_WORK?F01 S06+ ???????? Feature ???
- **Out:**
  - ?? Vulkan ????? `RND-F05`??? Feature ?????? Modern RHI / OpenGL ?????????? Granite?????????
  - ????? async compute / subpass merge / ?? history?Granite ???????????**?**?????????
  - ?? IR?Editor UI???????? Texture?Mesh GPU ???????????
  - ? Phase 1 ???????????????

## Reader quick start
1. ???**?????**?**?3.6 ????/????**????
2. [Implementation](./RND-F07_GRANITE_RDG_RESOURCE_REFACTOR_IMPLEMENTATION.md)?**??? DoD**?
3. Granite?`RenderGraph::bake` / `setup_attachments` / `RenderPassInterface`???????
4. ?????`ForwardRenderer.cpp`?`RegisterExternal*`??`SceneRenderTarget`?`ShadowResourceManager`?`RenderGraph/`?? Manual??

---

## 1) ?????

### Pain
- RDG ?????????????**????**? RT / ???Scene / ShadowManager ?????
- `RND-F01` ?? Bake?`a484daa`??????????? external ????????? Granite?
- ??????????????????????

### Goals
- **?? Granite RDG**?????? Pass ???Bake ?????? / ?? /????????transient ??????Execute ??????
- **???????**???????????????? external ??? Granite ?????? swapchain / ???????????**??**?Scene ?? SceneColor??
- **????????**?Phase 1 ???**???????????? RT**???????????

### Success
- Phase 1?????? **??** ??????? SceneColor/Depth?DirShadowAtlas?spot/point shadow?post buffer?`ForwardRenderer` ?????? Pass ???????????????smoke ?? render ???????
- Phase 2?????? Granite ?? API/???????????? clear/present ??? color pass?? **????????** ???????? Pass ???????? Manual `RegisterExternal` ?????? Bake ?????

---

## 2) ??

| ?? | ????? / ??? | ? Granite ?? |
|------|---------------------|-----------------|
| `SceneRenderTarget` | ?? SceneColor / SceneDepth | ? RT ??????? |
| `ShadowResourceManager` | `Ensure*` ?? atlas / spot / point | ???????????? |
| `ForwardRenderer` | `RegisterExternal*` + ????????? `Bake()` | external ?????Bake ? Granite |
| ? `RenderGraph/` | ???? + Manual ??? + ???? | ? `build_physical_*` / transient ?? |
| ???? / Mesh | ???? | **??**??? Feature ???? |

?????`a484daa`?experimental Bake??????

---

## 3) ??

### 3.1 ????????

```text
Phase 1  ?????? + ?????
    ? ?????? RT??????
    ?
Phase 2a ?? Granite ? RDG ??
    ? bake / physical / setup_attachments / execute
    ?
Phase 2b ?????? Pass ?????
    ? ???????? Manual ???
```

**?????** Phase 1 ? Renderer ?????????????????????Phase 2 ?????

### 3.2 Phase 1 ? ?????????

**???**????????????????????????????

**?? / ????????? Implementation??**
- ?? `SceneRenderTarget` ?????? `Initialize`/`Resize` ????????????????
- ?? `ShadowResourceManager::Ensure*` / Acquire ?????????????????
- ?? `ForwardRenderer` ? post ping-pong ???????
- `ForwardRenderer::Execute`????????**?**????????? Pass ???? clear ?? / ?? / ? Present?? Present ??? SceneColor???????????/Editor ????
- ? RDG?**???????**???????? Bake????????? external ????????????

**?????**
- RHI ???????????????????????????????
- ????????? dead / `#if 0` / ????? Phase 2 ????????Phase 1 DoD ?**??**????????????

### 3.3 Phase 2 ? Granite ? RDG ??????

#### 3.3.1 ??????? Granite?

| Granite | ?? | minEngine Phase 2 ?? |
|---------|------|------------------------|
| `RenderPassInterface` | `setup_dependencies` ??bake?? `setup` ? ?? `enqueue_prepare_render_pass` ? `build_render_pass` | ??????????? `IRenderPass`????? |
| `RenderPass::add_color_output` / `add_texture_input` / `set_depth_stencil_*` | ?????? + `AttachmentInfo` | ????????**??**???? `RHITexture*` |
| `RenderGraph::bake` | `filter_passes` ? `build_physical_resources` ? `build_physical_passes` ? `build_transients` ? `build_physical_barriers` ? ? | **??????**?????????????????**?????????** |
| `setup_attachments(device, swapchain)` | ??/???????? swapchain ??? | ????????? |
| `get_physical_texture_resource` | Bake ???? | Pass ???????? |
| `RenderPassExternalLockInterface` | ?????????? | **????**???????? SceneColor |

?????`D:\Dev\GitRepo\Granite\renderer\render_graph.hpp`?? `RenderGraph::bake` / `setup_attachments`??`render_graph.cpp`?`bake()` ????

#### 3.3.2 ??????????

1. **???????????**?Scene / ShadowManager ????? RT?
2. **????? Bake???????**???????? CreateTexture ? RegisterExternal????
3. **?? Granite ??????**?? Vulkan ?????? RHI ????? barrier ? GL ??????????????????minEngine ?? Bake ????
4. **? Manual ???? Bake**?Phase 2 ???????????????????? Granite ???
5. **???** Granite MIT?????????????????/?????? Progress/Design ???????

#### 3.3.3 ????????

???????Implementation ????????????????

1. ?????????? color?? depth?? bake ? setup_attachments ? ??/Present????????
2. SceneColor/Depth ???Sky/Opaque/Translucent ???
3. Post ping-pong ???
4. Shadow ???????/?? `ShadowResourceManager` ???????????/??????? Renderer??
5. ?? `RegisterExternal` ???????? Bake?

### 3.4 ??? Feature ??

| ID | ?? |
|----|------|
| `RND-F01` | Manual ? S0?S05 ??????**S06+ ?????? Feature ??**??? Bake ???????????F01 ??? Draft/Archived ???? Meta ?? Superseded by F07? |
| `RND-F06` | `ForwardRenderer` ???????F07 ???**??????**?????? `RenderPipeline`? |
| `RND-F03` / `RND-F05` | ??? Phase 1?Phase 2 ???? API ??? Modern RHI??? VK barrier/transient ?? F05 ????? |

### 3.5 ????????

```text
ForwardRenderer (????? Pass??????)
        ?
        ?
RenderGraph (Granite-style)
  add_pass / ?? AttachmentInfo
  bake() ? physical resources + order (+ barriers/transients as able)
  setup_attachments(rhi, swapchain?)
  enqueue / execute ? Pass::build_render_pass ?? get_physical_*
        ?
        ?
RHI CommandList / ??
```

Scene ?????????????**?**???????? SceneColor ???????

### 3.6 ??????????Phase 2 ???

> **???** ????????? Granite `render_graph.hpp`???????/???? **???????????**?
> **Phase 1?** ????????????????
> **?????** ??? `RHITexture` / `RHICommandList`???? Granite `Vulkan::ImageView` ????

#### 3.6.1 ????????

| Granite | minEngine | ?? |
|---------|-----------|------|
| `AttachmentInfo` | `RDGAttachmentInfo` | ????????????????? |
| `ResourceDimensions` | `RDGResourceDimensions` | Bake ????????? |
| `RenderResource` | `RDGResource` | ???????Texture / ?? Buffer? |
| `RenderTextureResource` | `RDGTextureResource` | ???? + ???? |
| `RenderPass`???? | `RenderPass` | ??? Pass ????? IO? |
| `RenderPassInterface` | `IRenderPass` | ????? |
| `RenderGraph` | `RenderGraph`???/??? Manual? | ? orchestrator |
| `get_physical_texture_resource` | `GetPhysicalTexture` | ?? `RHITexture*`?? `shared_ptr` ????? |

? Manual ? `RDGTextureRef` / `RenderGraphFrameResources::RegisterExternal` **?????**??????????????? ? ?????????????????????

#### 3.6.2 ???????

?? Granite `SizeClass` / `AttachmentInfo`?

```cpp
namespace minEngine
{
    enum class RDGSizeClass : uint8_t
    {
        Absolute,           // ????
        SwapchainRelative,  // ?? backbuffer / ??
        InputRelative,      // ??????????Granite size_relative_name?
    };

    enum class RDGAttachmentFlags : uint32_t
    {
        None = 0,
        Persistent = 1u << 0,   // Granite ATTACHMENT_INFO_PERSISTENT_BIT
        // InternalTransient ? Bake ??????? Pass ????
    };

    /** ???????Granite AttachmentInfo?? */
    struct RDGAttachmentInfo
    {
        RDGSizeClass SizeClass = RDGSizeClass::SwapchainRelative;
        float SizeX = 1.0f;
        float SizeY = 1.0f;
        std::string SizeRelativeName;   // InputRelative ???
        TextureFormat Format = TextureFormat::None; // UNDEFINED = ??/??????????????
        uint32_t Samples = 1;
        uint32_t Levels = 1;
        uint32_t Layers = 1;
        RDGAttachmentFlags Flags = RDGAttachmentFlags::Persistent;
        RHITextureDimension Dimension = RHITextureDimension::Texture2D;
    };

    /** Bake ?????????Granite ResourceDimensions ???? */
    struct RDGResourceDimensions
    {
        TextureFormat Format = TextureFormat::None;
        uint32_t Width = 0;
        uint32_t Height = 0;
        uint32_t Depth = 1;
        uint32_t Layers = 1;
        uint32_t Levels = 1;
        uint32_t Samples = 1;
        RDGAttachmentFlags Flags = RDGAttachmentFlags::None;
        RHITextureCreateFlags Usage = RHITextureCreateFlags::None; // Bake ??
        std::string DebugName;
    };
}
```

**???????** ??? `Absolute` + `SwapchainRelative`?`InputRelative` / history / storage image ????????????????????

---

#### 3.6.3 ????

```cpp
namespace minEngine
{
    class RDGResource
    {
    public:
        enum class Type : uint8_t { Texture, Buffer /* Phase2 ?? */, Proxy };
        static constexpr uint32_t kUnused = ~0u;

        Type GetType() const;
        uint32_t GetLogicalIndex() const;
        uint32_t GetPhysicalIndex() const;   // bake ?? kUnused
        void SetPhysicalIndex(uint32_t index);

        const std::unordered_set<uint32_t>& GetReadPasses() const;
        const std::unordered_set<uint32_t>& GetWritePasses() const;
        // written_in_pass / read_in_pass??? API ????
    };

    class RDGTextureResource : public RDGResource
    {
    public:
        void SetAttachmentInfo(const RDGAttachmentInfo& info);
        const RDGAttachmentInfo& GetAttachmentInfo() const;

        void AddUsage(RHITextureCreateFlags flags); // color/depth/srv ??
        RHITextureCreateFlags GetUsage() const;

        bool IsTransient() const;   // Bake: build_transients ??
        void SetTransient(bool enable);
    };
}
```

**????**
- ?????**??**??????`"SceneColor"`??Pass ???? API ?? `RDGTextureResource&`?
- **????** ?? `Bake()` ??????`BuildRenderPass` ???? bake ?????

#### 3.6.4 ? Pass ???????

?? Granite `RenderPass` ???????????

```cpp
namespace minEngine
{
    enum class RDGQueue : uint8_t
    {
        Graphics,   // ???? Graphics
        // Compute / AsyncCompute ? ??????? Granite ???
    };

    class RenderPass
    {
    public:
        const std::string& GetName() const;
        uint32_t GetIndex() const;
        RDGQueue GetQueue() const;

        // --- ?? API?setup_dependencies / IRenderPass::SetupDependencies ????---
        RDGTextureResource& SetDepthStencilOutput(const std::string& name, const RDGAttachmentInfo& info);
        RDGTextureResource& SetDepthStencilInput(const std::string& name);
        RDGTextureResource& AddColorOutput(const std::string& name, const RDGAttachmentInfo& info,
                                           const std::string& colorInput = {});
        RDGTextureResource& AddTextureInput(const std::string& name);

        // ?????????? stub??AddHistoryInput / AddResolveOutput / storage / buffer

        const std::vector<RDGTextureResource*>& GetColorOutputs() const;
        const std::vector<RDGTextureResource*>& GetTextureInputs() const;
        RDGTextureResource* GetDepthStencilOutput() const;
        RDGTextureResource* GetDepthStencilInput() const;

        void SetImplementation(std::unique_ptr<IRenderPass> impl);
        IRenderPass* GetImplementation() const;
    };
}
```

**`AddColorOutput(..., colorInput)`?** ? Granite ????`colorInput` ?????????????????Bake ?? **?????????**?rename??????? ping-pong ?? API?

#### 3.6.5 Pass ??????????

??? `IRenderPass::{Setup, PreparePass, BuildRenderPass}` ?? Manual builder ??????? Granite `RenderPassInterface`?

```cpp
namespace minEngine
{
    class RenderGraph; // ??
    class RenderPass;
    class RHI;
    class RHICommandList;

    class IRenderPass
    {
    public:
        virtual ~IRenderPass() = default;

        /** Bake ????????????? RenderPass ?? API??Granite: setup_dependencies */
        virtual void SetupDependencies(RenderPass& self, RenderGraph& graph) = 0;

        /** Bake ??????????? PSO/?????Granite: setup(device) */
        virtual void Setup(RHI& rhi) {}

        /** ?? CPU?? draw ????? UBO CPU ???? RHI ??????Granite: enqueue_prepare_render_pass */
        virtual void Prepare() {}

        /** ?? GPU ???Granite: build_render_pass */
        virtual void BuildRenderPass(RHICommandList& cmdList, RenderGraph& graph) = 0;

        /** ????? Pass?Granite: need_render_pass */
        virtual bool NeedRenderPass() const { return true; }

        virtual bool GetClearColor(uint32_t attachment, float outRGBA[4]) const { return false; }
        virtual bool GetClearDepthStencil(float& depth, uint8_t& stencil) const { return false; }
    };
}
```

| ?? | ?? | ?? | ?? |
|------|------|------|------|
| `SetupDependencies` | `Bake()` ????? | ?? IO | `RHICreateTexture`?????? |
| `Setup` | `Bake()` ? | ?? PSO ?????? | ?????????? RT |
| `Prepare` | ?? execute ? | ????????? | ???? RT ?? Scene ?? |
| `BuildRenderPass` | ?? | `graph.GetPhysicalTexture(...)` + RHI ?? | `RegisterExternal` / ?? Create ? RT |

#### 3.6.6 `RenderGraph` ?? API

```cpp
namespace minEngine
{
    class RenderGraph
    {
    public:
        void Reset();

        void SetBackbufferSource(const std::string& logicalTextureName); // Granite set_backbuffer_source
        void SetBackbufferDimensions(uint32_t width, uint32_t height);   // ?? SwapchainRelative

        RenderPass& AddPass(const std::string& name, RDGQueue queue = RDGQueue::Graphics);
        RenderPass* FindPass(const std::string& name);

        RDGTextureResource& GetTextureResource(const std::string& name); // ????????????Bake ?? Find

        /** Granite bake()????? ? ???? ?????pass merge/transient/barrier ? ?? Pass::Setup */
        void Bake();

        /**
         * Granite setup_attachments(device, swapchain_view)?
         * ? physical_dimensions ??/?? RHITexture??? swapchain ?????? alias??
         * GL ???swapchain ??????? Present ????? blit Pass?
         */
        void SetupAttachments(RHI& rhi, RHITexture* swapchainOrNull);

        /** ? bake ???? Pass / ????? NeedRenderPass ? Prepare ? BuildRenderPass */
        void EnqueueRenderPasses(RHICommandList& cmdList);

        RHITexture* GetPhysicalTexture(const RDGTextureResource& resource);
        RHITexture* GetPhysicalTexture(uint32_t physicalIndex);
        RHITexture* TryGetPhysicalTexture(RDGTextureResource* resource);

        const RDGResourceDimensions& GetPhysicalDimensions(uint32_t physicalIndex) const;
        bool IsBaked() const;

    private:
        // --- Bake ????? Granite ????????????---
        // validate_passes;
        // traverse_dependencies / filter_passes / reorder_passes;
        // build_physical_resources;
        // build_physical_passes;      // ??? no-op merge
        // build_transients;           // ??????? Persistent
        // build_render_pass_info;     // ??? RHIRenderPassInfo
        // build_barriers / build_physical_barriers; // GL ?????
        // build_aliases;
        // ?? foreach: impl->Setup(rhi);

        std::vector<std::unique_ptr<RenderPass>> m_Passes;
        std::vector<std::unique_ptr<RDGResource>> m_Resources;
        std::unordered_map<std::string, uint32_t> m_NameToResource;

        std::string m_BackbufferSource;
        uint32_t m_BackbufferWidth = 0;
        uint32_t m_BackbufferHeight = 0;

        std::vector<uint32_t> m_PassStack;                 // bake ???????? pass index?
        std::vector<RDGResourceDimensions> m_PhysicalDims;
        std::vector<std::shared_ptr<RHITexture>> m_PhysicalTextures; // ???
        // std::vector<PhysicalPass> m_PhysicalPasses;    // merge ??
        uint32_t m_SwapchainPhysicalIndex = RDGResource::kUnused;
        bool m_IsBaked = false;
    };
}
```

**?? API ????**
- ????????`RegisterExternalTexture`?`SetPassExecutionOrder` ???????????`RenderGraphFrameResources` ?????
- `Bake()` **??**???????????? `m_PhysicalTextures`?????????

---

#### 3.6.7 Bake ?????????????????

?? `Granite::RenderGraph::bake()`?`render_graph.cpp`??

```text
1. ?? Pass?SetupDependencies??? IO??? read/write sets?
2. validate_passes
3. ? backbuffer_source ? write_passes ?? traverse_dependencies
4. reverse + filter_passes??? reorder_passes
5. build_physical_resources   ? ????? index?rename ??
6. build_physical_passes      ? ??? 1:1?? merge
7. build_transients           ? ????? transient
8. build_render_pass_info
9. build_barriers????
10. swapchain alias ???????
11. build_physical_barriers / build_aliases?GL ?????
12. ?? Pass?Setup(rhi)
```

**?????** ????????????????? Deferred ??????????? indegree??? Bake?

#### 3.6.8 ?????Renderer ???

```text
// ????????????
graph.Reset();
???AddPass + SetImplementation?SetBackbufferSource("SceneColor" ??? Present ??);
graph.SetBackbufferDimensions(w, h);
graph.Bake();
graph.SetupAttachments(rhi, swapchainTexOrNull);

// ??????????? Bake?????? Bake??
graph.SetupAttachments(...);   // Granite ???? setup??????
cmd.Begin...
graph.EnqueueRenderPasses(cmd);
cmd.End / Present
```

`ForwardRenderer` ???**?? Pass???????? UBO**???? `RHICreateTexture2D` ? SceneColor?

#### 3.6.9 ????????????

```cpp
// IRenderPass for opaque
void OpaquePass::SetupDependencies(RenderPass& self, RenderGraph& graph)
{
    RDGAttachmentInfo color{};
    color.SizeClass = RDGSizeClass::SwapchainRelative;
    color.Format = TextureFormat::RGBA16F;
    color.Flags = RDGAttachmentFlags::Persistent;

    RDGAttachmentInfo depth{};
    depth.SizeClass = RDGSizeClass::SwapchainRelative;
    depth.Format = TextureFormat::DEPTH24STENCIL8;

    self.AddColorOutput(kRDGSceneColor, color);
    self.SetDepthStencilOutput(kRDGSceneDepth, depth);
    self.AddTextureInput(kRDGDirShadowAtlas); // ??? Shadow Pass ??
}

void OpaquePass::BuildRenderPass(RHICommandList& cmd, RenderGraph& graph)
{
    RHITexture* color = graph.GetPhysicalTexture(graph.GetTextureResource(kRDGSceneColor));
    RHITexture* depth = graph.GetPhysicalTexture(graph.GetTextureResource(kRDGSceneDepth));
    // BeginRenderPass(color, depth); draw...
}
```

#### 3.6.10 ??????? Granite ??????????

| ? | Granite | F07 ?? |
|----|---------|----------|
| Buffer / storage image | `RenderBufferResource` ? | Deferred |
| History | `add_history_input` | Deferred |
| Subpass merge | `build_physical_passes` merge | ?? 1:1 |
| Async compute / semaphore | queue flags | Deferred ? F05 |
| `RenderPassExternalLockInterface` | ????? | ????/cluster ???????**?**?? SceneColor |
| ?? automatic barrier | `build_physical_barriers` | GL ?????????? |

#### 3.6.11 ???????

| ?? | ?? |
|------|------|
| ? `IRenderPass`?Manual ?????? | ??? Granite ???? `IRenderPass`?`SetupDependencies` / `Setup` / `Prepare` / `BuildRenderPass`? |
| `RenderPassBuilder` / `PassResourceAccess` | ???? `RenderPass` ???Builder ?? |
| `RDGTextureDesc`?? W/H? | ? `RDGAttachmentInfo` ?? |
| `RenderGraphFrameResources` | ????? `RenderGraph`??????????? |
| `SceneRenderTarget` | ?????????????? Renderer/`SetBackbufferDimensions` |
| `ShadowResourceManager` ?? Ensure | ???????/??????? Renderer |

---

## 4) ????

| ?? | ?? | ?? | ?? |
|------|------|------|------|
| A. ? external ????? Bake | ?????? | ???? Granite | **??** |
| B. Phase 1 ?? Renderer ???? | ???? | ???????Phase 2 ?? | **??**??????? |
| C. Phase 1 ????? ? Phase 2 Granite ????? | ??????? | ?????? | **??** |
| D. ??????????????? | ???? | ???????? external | **??**?????????????? |

---

## 5) ?????

| ?? | ?? | ?? |
|------|------|------|
| Phase 1 ????????????? | ???/?????? | ?????? **??? RT / shadow map / post buffer**??? `Texture`/`StaticMesh` GPU ???? |
| Phase 2????????? | ?? F01 Bake | Bake ?????? Granite `bake()`?Design ???????? |
| GL ?????? barrier/transient | ???? | ?????????????????????????? F05 |
| ???????? | ??/???? | Implementation ??????????? Pass |
| ?????? Granite ? RHI ??? | ???? | ???**??? Bake ??**???????? minEngine RHI |

---

## 6) ????

### Phase 1
- [x] ???? **?** SceneColor/Depth?shadow atlas/maps?post ?? RT ?????????/??/????????????????
- [x] `ForwardRenderer`????????**??**????????????/??/?????
- [x] Editor/????????????????/?????
- [x] `minEngineTests test smoke` ?**?????**??????`render-graph` / ??? RT ?????????? `SKIP`??? Progress ????

### Phase 2
- [ ] ?? Granite ???????`add_pass`??????`bake`?`setup_attachments`??????????????????
- [x] ???????**????? bake/setup ??**?? Scene/ShadowManager `CreateTexture`??????????? GPU ???????? Present??
- [ ] ?????????Scene/Shadow **??**??? RT ??????? `RegisterExternal` ?????????? external ???
- [ ] ???F01 S06+ ????? F07 ???ACTIVE_WORK ???? F07?

---

## 7) Status note

**In Progress** ? Phase 1 **S01?S03 Done**???? **S04** Granite ????????RenderPass / IRenderPass?

---

## ????

| ?? | ?? |
|------|------|
| 2026-08-02 | ??????????Phase 1 ??????Phase 2 Granite ??????? `D:\Dev\GitRepo\Granite` |
| 2026-08-02 | ?? ?3.6?Attachment/????/RenderPass/IRenderPass/RenderGraph API?Bake ?????????? |
| 2026-08-02 | ???????? `RenderPass`????? `IRenderPass`??? Granite `RenderPass` / `RenderPassInterface`??Status ? In Progress |
